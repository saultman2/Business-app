kind = "web"
previewPath = "/"
title = "BuildPro - Construction Business Manager"
version = "1.0.0"
id = "artifacts/construction-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26010

[services.development]
run = "pnpm --filter @workspace/construction-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/construction-app", "run", "build" ]
publicDir = "artifacts/construction-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26010"
BASE_PATH = "/"
